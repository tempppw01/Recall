# 0.0.1 任务清单（唯一事实来源）

> 目标：MVP 稳定化（核心流程可用 + 关键边界收敛 + 可可靠部署）

## 进度

- ✅ 已完成（本计划阶段已完成；历史发布记录见 `docs/plan/versioning-policy.md`）

## 任务列表

### A) 计划文件整理（仓库整洁）

- ✅ 将历史计划文件迁移到 `docs/plan/archive/`（DoD：原 docs/ 下不再散落 PLAN/TODO 文件，仅保留指针）
- ✅ 在 `docs/PLAN.md` 保留简短指向（DoD：入口清晰、无重复维护）

### B) AI 管理助手：推荐“直接操作”（Phase 2 补完）

- ✅ 定义动作协议（DoD：recommendations 包含 suggestedPriority/suggestedPinned/suggestedDuePreset 规范）
- ✅ 后端 manage-agent 输出扩展（DoD：返回字段可解析且有校验/收敛）
- ✅ 前端推荐卡片增加按钮（DoD：置顶/优先级/日期一键操作可用）
- ✅ 推荐执行后本地 +（若配置）PG 同步正确（DoD：执行后刷新与状态一致）
- ✅ 文档补充（DoD：README 中有说明）

### C) Prisma / 数据库边界收敛

- ✅ 动态 PG 仅在未登录时生效 + session 优先（DoD：所有相关 API 路由一致）
- ✅ 限制 Prisma 动态连接参数（DoD：connection_limit/connect_timeout/pool_timeout 可配置）
- ✅ DB 验证脚本与文档（DoD：docs/plan/database.md + scripts/db-check.sh 可运行）

### D) 时间轴页 Phase 2（体验增强）

- ✅ 日期分组层级强化 + sticky（DoD：日期 header 视觉更明显）
- ✅ 状态标签秒懂（DoD：图标+颜色区分）
- ✅ 卡片可读性增强（DoD：hover 反馈/间距）
- ✅ 节点动效/轴线视觉（DoD：进入动画不影响性能）
- ✅ 展开/收起过渡（DoD：过渡顺滑）
- ✅ 滚动定位感增强（DoD：今天提示可见）

### E) 工程化与发布

- ✅ GitHub Release 创建（DoD：release notes 存档且与 Release 对应）
- ✅ Docker 构建并推送（DoD：推送版本 tag + latest，并完成最小启动验证）
