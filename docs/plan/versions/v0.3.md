# v0.3 任务清单（规划，未开工）

> 说明：v0.3 为规划版本，未进入执行前请勿开工。

## 目标

- 同步可靠性与可观测性提升（减少“我明明做了但没同步上”的不确定性）

## 任务列表

- ✅ 明确 sync 模型与冲突策略（DoD：新增 `docs/plan/sync.md` + 服务端返回 `conflicts` 摘要）
- ⬜ 引入更清晰的错误码/日志字段（DoD：API 关键路径可定位）
- ⬜ 增加可选的“深度健康检查 /api/health?deep=1”（DoD：可配置启用，默认仍为浅检查）
