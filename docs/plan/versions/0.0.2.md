# 0.0.2 任务清单（仅规划，禁止开工）

> 前提：只有在 `docs/plan/versions/0.0.1.md` 全部 ✅、最近审计无未处理高危问题、CHANGELOG 与 Release notes 一致之后，才允许切换到 0.0.2。

## 目标

- 在 0.0.1 稳定基线之上，提升扫描效率、视觉反馈和工程可维护性。

## 任务列表

### A) 页面结构与视觉收敛

- ⬜ 左侧导航层级优化（DoD：导航分区、图标、计数、选中态更清晰）
- ⬜ 页面 Header 重构（DoD：标题、说明、快捷操作结构统一）
- ⬜ 主内容区骨架优化（DoD：Header / Summary / Controls / Content 结构稳定）
- ⬜ 列表 / 卡片信息排布优化（DoD：重点信息前置、层级更清晰）
- ⬜ 新建任务入口改为卡片式创建（DoD：去掉当前直接新建样式，创建态与任务卡片体系一致）
- ⬜ 深色主题统一（DoD：背景、边框、文本层级一致）

### B) 工程与发布

- ⬜ 引入 Prisma migrations 并固化 migrate deploy（DoD：纳入 `prisma/migrations/`，并补 deploy 文档）
- ⬜ 增加最小健康检查端点（DoD：Docker 启动后可通过固定命令验证服务健康）

## 候选改进建议

### 高优先级
- 补一份 0.0.2 性能预算与回归门槛说明
- 明确视觉增强与可访问性之间的平衡原则

### 中优先级
- 为体验增强补充最小实机回归清单
- 给关键页面增加更统一的空态 / 骨架态策略

### 低优先级
- 为阶段总结增加可配置时间范围
