# v0.1 任务清单（唯一事实来源）

> 目标：先把 Recall 的计划治理、基础稳定性和首个规范化发布基线收拢到可执行状态。
>
> 说明：仓库历史上已存在 `v0.2.x / v0.3.x` 标签与 Release，这是既有事实；本文件定义的是**当前要求下的规范化计划基线**。在 `v0.1` 全部 ✅、最近一轮审计无未处理高危问题、CHANGELOG 与 Release notes 一致之前，不推进 `v0.2`。

## 进度

- 完成度：**4/4**
- 当前状态：**已完成，待按新治理规则判断发版动作**

## 任务列表

### A) 计划目录治理收敛

- ✅ 建立 `docs/plan/README.md / roadmap.md / versions/ / audits/ / releases/` 主结构  
  DoD：导航完整、入口统一、后续新增计划不再散落到仓库根目录或 `docs/` 根目录。
- ✅ 将当前执行版本唯一事实来源切换到 `docs/plan/versions/v0.1.md`  
  DoD：README、roadmap、CHANGELOG、版本导航均以 `v0.1/v0.2/v0.3/v1.0` 表述。

### B) 基础稳定性修复

- ✅ 修复 `src/app/page.tsx` 中 `statusFeedback` 在声明前被 `useEffect` 引用导致的 TypeScript 报错  
  DoD：`npm run typecheck` 通过；不存在 `TS2448 / TS2454`。

### C) 审计闭环

- ✅ 为本轮变更补齐审计报告  
  DoD：新增本轮审计文件，覆盖安全 / Bug / UI / 可维护性四类检查，并给出高/中/低结论。

## 发布退出条件

只有在以下条件全部满足后，才允许从 `v0.1` 推进到 `v0.2`：

- 本文件中所有任务项均为 ✅
- 最近一轮审计报告无“未处理高危问题”
- `docs/plan/releases/CHANGELOG.md` 与对应 Release notes 一致
- 已按发布规则确认版本基线与允许的下一个 tag

## 候选改进建议（转入 v0.2 规划）

### 高优先级
- 清理或归档旧的 `0.0.x` 版本文件，避免双轨版本文件长期并存造成误读
- 为版本切换新增一份“历史 tag 与新治理版本线”的操作说明

### 中优先级
- 给 `docs/plan/README.md` 增加自动统计口径（完成数 / 总数）
- 补充一份发版前检查清单模板

### 低优先级
- 给审计模板增加“修复验证命令”字段
