# 0.0.3 任务清单（已启动执行）

> 前提：只有在 0.0.2 完成并完成对应发版后，才允许切换到 0.0.3。

## 目标

- 增强功能视图能力，让 Recall 从基础任务工具进一步走向完整效率系统。

## 执行状态

- 当前状态：已启动
- 当前轮次：Round 59
- 当前切入点：Review / 检查页第一版骨架（导航入口、主列表区、详情区、占位交互）

## 任务列表

### A) 功能页面增强

- ⬜ 增加检查功能页面（Review / 检查视图）（DoD：具备独立工作流入口、列表区与详情区结构）
- ⬜ 增加附近页面（Nearby / 附近可完成任务）（DoD：可根据位置或地点标签展示附近适合完成的任务）
- ⬜ 强化任务上下文视图（DoD：按时间、位置、标签、状态切换更顺畅）

### B) 同步与可靠性增强

- ⬜ 明确 sync 模型与冲突策略（DoD：文档清晰描述成功 / 冲突 / 覆盖 / 重试路径）
- ⬜ 统一错误码 / requestId / 结构化日志（DoD：关键 API 可按入口、阶段、结果排查）
- ⬜ 增加深度健康检查（DoD：默认浅检查，深度模式覆盖 DB / sync 关键依赖）

## 候选改进建议

### 高优先级
- 为检查页 / 附近页补可执行交互流程图
- 建立同步异常复现与回归脚本

### 中优先级
- 优化上下文任务视图之间的切换逻辑
- 优化同步失败时的用户提示文案

### 低优先级
- 为诊断输出增加分级建议
