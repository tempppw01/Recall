# v0.2 任务清单（规划，未开工）

> 说明：v0.1 全部 ✅ 并满足退出条件前，本文件仅允许规划，不允许开工。

## 时间轴 Phase 3（高级视觉）

- ⬜ 探索瀑布流布局（DoD：在至少一种屏幕尺寸下可用，滚动性能不明显退化）
- ✅ 支持时间热力 / 完成密度感知（DoD：时间轴顶部展示近28天完成密度热力格）
- ✅ 支持阶段总结（本周 / 本月完成情况）（DoD：时间轴顶部展示完成数/完成率/Top 类目）
- ✅ 与主题色 / 渐变系统联动（DoD：时间轴总结条、热力格、轴线跟随全局 theme 变量）

## 工程与发布

- ✅ 引入 Prisma migrations 并固化 migrate deploy（DoD：已纳入 `prisma/migrations/` + 文档说明 + deploy 指令）
- ✅ 增加最小健康检查端点（DoD：Docker 启动后可用 curl 验证；`/api/health` 返回 ok）
