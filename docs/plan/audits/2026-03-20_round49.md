- 日期：2026-03-20
- Round：round49
- 覆盖范围：计划目录收敛（docs/plan 导航、roadmap、版本文件、历史计划归档）
- 关联提交：c1b8b55 docs(v0.1): realign planning structure to docs/plan rules

## 1) 漏洞/安全

- 高危：无。
- 中危：无新增安全风险；本轮仅为文档与目录调整。
- 低危：无。

## 2) Bug / 边界条件

- 高危：无。
- 中危：
  - 发现计划体系与用户强制规则不一致：`docs/plan/README.md` 指向 `v1.5`，而 `docs/PLAN.md` 指向 `v0.1`，容易导致后续执行版本判断错误。
  - 处理：本轮已统一回 `v0.1` 作为当前工作版本，并将 `v0.2/v0.3/v1.0` 明确为“仅规划，禁止开工”。
- 低危：
  - 历史发布 tag 已存在 `v0.2.x/v0.3.x`，与当前强制流程存在历史不一致。
  - 处理：本轮在 README / roadmap 中保留说明，但未擅自改 tag；后续发版时需继续显式说明。

## 3) UI / 交互（如适用）

- 高危：无。
- 中危：无。
- 低危：无（本轮不涉及产品 UI 代码）。

## 4) 可维护性 / 可观测性

- 高危：无。
- 中危：
  - `docs/` 根目录仍散落计划性文档，不符合“所有任务/计划/审计/发布记录只能放在 docs/plan/ 下”的规则。
  - 处理：本轮已将 `docs/UI_BUTTON_SYSTEM.md`、`docs/CALENDAR_UI_0.1.2.md` 迁移到 `docs/plan/archive/`。
- 低危：
  - `v1.4/v1.5` 等历史版本文件仍保留在 `docs/plan/versions/`，但已不作为当前执行入口。
  - 建议：后续可视情况决定是否迁移到 archive 或保留为历史记录。

## 结论

- 高危 0（无）/ 中危 2 / 低危 2
- 是否阻塞发版：否（当前为计划治理轮，不涉及运行时代码）；但若要严格按新规则继续发版，必须持续以 `v0.1` 为唯一执行入口。
- 后续建议：
  1. 下一轮优先核对 `v0.1` 是否已满足退出条件，并明确“历史 tag 与当前规则”的处理口径。
  2. 若继续清理计划体系，可评估 `v1.4/v1.5` 等历史执行文件是否需要归档或增加“历史版本，仅供参考”标记。
