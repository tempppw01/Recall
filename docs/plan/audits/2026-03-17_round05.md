# 审计报告（2026-03-17_round05）

- 日期：2026-03-17
- Round：round05
- 覆盖范围：Prisma migrations 纳入版本控制 + 文档/脚本修订
- 关联提交：<待本轮 commit 后补充>

## 1) 漏洞/安全

- 高危：无
- 中危：
  - migrations 中包含建表 SQL：属于预期，但需确保不泄露敏感信息。
  - 检查：当前迁移仅为 schema 结构，不含业务数据。
- 低危：无

## 2) Bug / 边界条件

- 中危：
  - 已存在数据库（通过 db push 创建）切换到 migrations 流程时，可能出现重复建表或状态不一致。
  - 处理：在 `docs/plan/database.md` 增加“基线接入”说明（`migrate resolve --applied 20260317_init`）。

## 3) UI / 交互
- 不涉及

## 4) 可维护性 / 可观测性

- 中危：
  - 若没有 `migration_lock.toml`，`migrate deploy` 行为可能不稳定。
  - 处理：补充 `prisma/migrations/migration_lock.toml` 固化 provider。

## 结论

- 高危 0 / 中危 3（均已处理/说明）/ 低危 0
- 是否阻塞发版：否
