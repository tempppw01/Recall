- 日期：2026-03-20
- Round：round50
- 覆盖范围：v0.1 退出条件核对、历史 tag 与当前规则的版本口径文档化
- 关联提交：d4baaf6 docs(v0.1): document versioning policy for historical tags

## 1) 漏洞/安全

- 高危：无。
- 中危：无。
- 低危：无（本轮仅涉及文档治理与发布流程口径）。

## 2) Bug / 边界条件

- 高危：无。
- 中危：
  - 发现 `v0.1` 的任务项虽然全部 ✅，但仓库历史 tag 已到 `v0.3.1`，与“必须从 v0.1 开始逐步推进”的当前规则存在语义冲突。
  - 处理：本轮新增 `docs/plan/versioning-policy.md`，明确“历史发布事实保留、当前计划治理按 v0.1 单线执行”的默认口径。
- 低危：
  - `CHANGELOG` 中没有 `v0.1.0` 的正式发布记录；若以后需要严格按当前规则对外解释，需要继续沿用统一说明模板。

## 3) UI / 交互（如适用）

- 高危：无。
- 中危：无。
- 低危：无（本轮不涉及 UI 代码）。

## 4) 可维护性 / 可观测性

- 高危：无。
- 中危：
  - 如果不把版本口径单独文档化，后续每轮审计 / 发版 /阶段报告都需要重复解释一次，容易产生新偏差。
  - 处理：已在 `docs/plan/README.md` 导航中加入 `docs/plan/versioning-policy.md`。
- 低危：
  - `v1.4`、`v1.5` 等历史计划文件仍在 `docs/plan/versions/` 内保留，当前不构成执行冲突，但可能给新参与者造成误读。
  - 建议：后续可为这些文件标题补“历史版本，仅供参考”，或统一迁入 archive。

## 结论

- 高危 0（无）/ 中危 2 / 低危 2
- 是否阻塞发版：部分阻塞。若按当前新规则追求“口径完全一致”，还缺少对 `v0.1.0` 未实际存在的最终处理决策；若采用默认方案 A（保留历史 tag，不重写历史），则不阻塞继续推进计划治理。
- 后续建议：
  1. 默认采用 `versioning-policy.md` 中的方案 A，并在后续阶段报告中持续沿用。
  2. 下一轮可继续清理 `docs/plan/versions/` 下的历史执行文件标识，避免误用。
