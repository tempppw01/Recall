# 审计报告（2026-03-17_round09）

- 日期：2026-03-17
- Round：round09
- 覆盖范围：Docker / 部署安全（NEXTAUTH_SECRET 默认弱值）
- 关联提交：<待本轮 commit 后补充>

## 1) 漏洞/安全

### 高危
- Dockerfile 内置默认 `NEXTAUTH_SECRET="change-me-in-prod"`。
  - 风险：若使用者未覆盖，会在生产环境使用已知弱默认值，影响会话与认证安全。
  - 处理：已修复。
    - 移除 Dockerfile 弱默认值
    - 新增容器启动检查：生产环境下若 `NEXTAUTH_SECRET` 缺失或仍为默认值，则拒绝启动
    - `docker-compose.yml` 改为强制要求显式提供 `NEXTAUTH_SECRET`

### 中危
- README / compose 示例会影响用户实际配置习惯。
  - 处理：示例已改成 `<set-a-strong-secret>`，不再提供误导性默认值。

### 低危
- 无

## 2) Bug / 边界条件
- 高危：无
- 中危：
  - 启动脚本依赖 shell 环境；当前基于 Alpine `/bin/sh`，兼容。
- 低危：
  - 错误提示里用了 openssl 示例；若运行环境没有 openssl，只影响提示，不影响启动逻辑。

## 3) UI / 交互
- 不涉及

## 4) 可维护性 / 可观测性
- 中危：
  - 安全配置若只写文档，不做 fail-fast，容易再次回归。
  - 处理：已改为启动时强校验。
- 低危：
  - 后续可增加启动日志中的配置诊断（已隐藏敏感值）。

## 结论
- 高危 1（已修复） / 中危 3（已处理） / 低危 1
- 是否阻塞发版：否（已同轮修复）
