- 日期：2026-03-20
- Round：round54
- 覆盖范围：Docker 发布策略调整为默认全量多架构（workflow + 文档）
- 关联提交：待本轮提交后补充

## 1) 漏洞/安全

- 高危：无。
- 中危：无。
- 低危：无（本轮主要调整 CI/CD 构建平台策略）。

## 2) Bug / 边界条件

- 高危：无。
- 中危：
  - 将 main 分支 push 也改成默认构建 `linux/amd64,linux/arm64` 后，CI 时长和失败概率可能高于此前仅构建 amd64 的策略。
  - 处理：本轮按用户明确要求切为默认全量多架构；若后续 CI 时间过长，再考虑优化缓存或 runner 策略，而不是回退需求。
- 低危：
  - 现有文档原先仍写着“main 默认仅 amd64”，容易和 workflow 真实行为不一致。
  - 处理：本轮已同步更新 `docs/DOCKER_PUBLISH.md`。

## 3) UI / 交互（如适用）

- 高危：无。
- 中危：无。
- 低危：无（本轮不涉及产品 UI）。

## 4) 可维护性 / 可观测性

- 高危：无。
- 中危：
  - 如果 workflow 和文档不同步，后续排查 Docker 发布问题时会产生误导。
  - 处理：本轮同时修改了 `.github/workflows/docker-publish.yml` 与 `docs/DOCKER_PUBLISH.md`。
- 低危：
  - 当前未新增 workflow 级 smoke test，仅修改了平台选择逻辑；后续如需更稳，可考虑增加 manifest 或 buildx inspect 校验步骤。

## 结论

- 高危 0（无）/ 中危 2 / 低危 2
- 是否阻塞发版：否（本轮为发布策略对齐，可直接进入后续发布流程）
- 后续建议：
  1. 下次 Docker 发版时关注 main 分支构建耗时是否明显上升。
  2. 如后续需要，可补一个“构建产物 manifest 校验”步骤，进一步确认多架构镜像已正确推送。
