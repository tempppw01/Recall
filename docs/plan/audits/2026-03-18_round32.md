# 审计报告（2026-03-18_round32）

- 日期：2026-03-18
- Round：round32
- 覆盖范围：用户集中反馈修复（移动端黑块/天气定位/默认入口/AI 管理助手调用）
- 关联提交：<待本轮 commit 后补充>

## 1) 漏洞/安全
- 高危：无
- 中危：
  - 管理助手前端强依赖本地 API Key，导致“有服务端 key 但仍无法调用”场景失败。
  - 处理：移除前端硬阻断，允许走服务端 key fallback（后端已有兜底）。
- 低危：
  - 天气定位引入地理位置权限请求，属于用户授权行为，需前端明确提示（已通过按钮显式触发）。

## 2) Bug / 边界条件
- 高危：无
- 中危：
  - 移动端底部黑块/布局需上拉问题，来源于底部留白与容器高度策略不协调。
  - 处理：收敛底部 padding，避免视觉黑块与不必要上拉。
- 低危：
  - 定位失败时依赖第三方 reverse geocode 结果，已提供 fallback 文案与本地坐标兜底。

## 3) UI / 交互
- 高危：无
- 中危：
  - 默认首页切换为 AI 助手、快捷入口默认折叠，符合当前用户操作偏好。
- 低危：
  - 仍可在设置中增加“默认首页”可配置开关，后续迭代可做。

## 4) 可维护性 / 可观测性
- 高危：无
- 中危：
  - 新增 `/api/weather/reverse` 使天气定位流程完整，可与现有 search/forecast 形成闭环。
- 低危：
  - 建议后续记录定位失败的错误类型分布，用于优化提示语。

## 结论
- 高危 0 / 中危 4 / 低危 3
- 是否阻塞发版：否
