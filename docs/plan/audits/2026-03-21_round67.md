- 日期：2026-03-21
- Round：round67
- 覆盖范围：GitHub 对外版本线与内部 0.0.x 计划线映射，明确 v0.3.2 候选范围
- 关联文件：
  - docs/plan/versioning-policy.md
  - docs/plan/releases/v0.3.2_candidate_scope.md
  - docs/plan/releases/CHANGELOG.md

## 1) 漏洞/安全

- 高危：无。
- 中危：无。
- 低危：无（本轮仅补充版本治理与 release 规划文档，不涉及运行时代码）。

## 2) 流程 / 执行问题

- 高危：
  - 当前内部 0.0.x 计划线与 GitHub 对外 0.3.x 发布线并存，但之前缺少明确映射规则，容易导致“内部轮次”和“外部版本”混淆。
  - 处理：本轮正式重写 `versioning-policy.md`，明确两条线的职责差异与默认映射方式。
- 中危：
  - 下一次正式发布虽然方向已逐步显现，但没有独立的候选范围文档，难以判断“哪些该进 0.3.2”。
  - 处理：本轮新增 `v0.3.2_candidate_scope.md`。
- 低危：
  - `CHANGELOG` 之前没有“Next / Planned”区段，导致当前发布准备状态不可见。
  - 处理：本轮补齐 `v0.3.2` 计划入口。

## 3) 文档 / 规划一致性

- 高危：无。
- 中危：
  - 过去文档里曾把 `0.0.x` 说成唯一主线，但现在实际已经进入“内部计划线 + 外部发布线”并行模式。
  - 处理：本轮将该口径正式升级，避免后续继续沿用旧表述。
- 低危：
  - `v0.3.2` 当前仍是候选阶段，不应被误判为立即可发布版本。

## 4) 可维护性 / 可观测性

- 高危：无。
- 中危：
  - 没有 release 候选范围时，后续每轮都需要重新判断是否并入发布，维护成本高。
  - 处理：本轮把候选范围单独沉淀成文件。
- 低危：
  - 后续仍可进一步补 release checklist / 发布前验收模板，以降低正式发版成本。

## 5) 本轮收口

- 本轮实际落地内容：
  - 明确了 `0.0.x` 内部计划线与 `0.3.x` 外部发布线的映射规则。
  - 确定下一次 GitHub 正式发布目标为 `v0.3.2`。
  - 建立了 `v0.3.2` 候选范围与 changelog 入口。
- 验证情况：
  - 本轮为纯文档修复，不涉及应用代码、构建产物或运行时逻辑；无需额外执行 `npm run build`。
  - 当前适合作为一次独立 docs 小步提交。
- 提交建议：
  - 可以直接 `add / commit / push`。
  - 后续等 Review 状态反馈层收口后，再正式判断是否满足 `v0.3.2` 发版门槛。

## 结论

- 高危 1 / 中危 4 / 低危 4
- 是否阻塞发版：否（本轮是为发版做准备，不阻塞现有线上状态）
- 后续建议：
  1. 继续推进 Review 状态反馈层收口。
  2. 收口后回到 `v0.3.2_candidate_scope.md` 做最终纳入判断。
  3. 补正式 release notes 与 release checklist，再发 `v0.3.2`。
